language: cpp

sudo: required
dist: trusty

addons:
 apt:
  packages:
  - build-essential
  - qtbase5-dev
  - devscripts
  - dpkg-dev
  - debhelper
  - fakeroot

script:
- make

notifications:
 email: false
