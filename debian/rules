#!/usr/bin/make -f

%:
	dh $@ --parallel

override_dh_auto_configure:

override_dh_auto_clean:
	dh_auto_clean -- -f make.mk

override_dh_auto_build:
	dh_auto_build -- -f make.mk

override_dh_auto_install:
	dh_auto_install -- -f make.mk
